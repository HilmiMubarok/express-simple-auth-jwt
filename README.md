## SIMPLE AUTH API with JWT using Express, Mongoose, And MongoDB

### Installation
    - ``npm install``
    - rename ``.env.example`` to ``.env``
    - edit ``.env`` file and add your MongoDB connection string